pipeline {
    agent {
        docker { 
            image 'node:18-bullseye' // This image already has Node.js installed
        }
    }
    stages {
        stage('test') {
            steps {
                sh 'node --version'
            }
        }
    }
}